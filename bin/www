#!/usr/bin/env node
import app from '../app.js';
import db from '../scripts/db.js';

await db.connect();
const port = 3000;
app.listen(port, () => {
    console.log(`Port ${port} has been listening.`);
});